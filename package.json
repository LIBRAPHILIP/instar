{
  "name": "instar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Instar — self-molting autonomic governor on GenLayer. Halts, tunes, and rewrites itself with no vote.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/genome.node.test.js",
    "deploy": "node scripts/deploy.mjs studionet",
    "deploy:studionet": "node scripts/deploy.mjs studionet",
    "deploy:bradbury": "node scripts/deploy.mjs bradbury"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8",
    "viem": "^2.55.10"
  }
}
